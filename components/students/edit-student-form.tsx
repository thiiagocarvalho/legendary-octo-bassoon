'use client';
import { FormEvent,useState } from 'react';
import { useRouter } from 'next/navigation';
type Student={id:string;fullName:string;phone:string;birthDate:Date};
export function EditStudentForm({student}:{student:Student}){const router=useRouter();const [message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch(`/api/admin/students/${student.id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});setMessage(r.ok?'Cadastro atualizado.':'Confira os dados e tente novamente.');if(r.ok)router.refresh();}return <form className="grid gap-3 rounded-xl border bg-white p-4 md:grid-cols-3" onSubmit={submit}><input aria-label="Nome completo" className="rounded-lg border px-3 py-2" defaultValue={student.fullName} name="fullName" required/><input aria-label="Telefone" className="rounded-lg border px-3 py-2" defaultValue={student.phone} name="phone" required/><input aria-label="Data de nascimento" className="rounded-lg border px-3 py-2" defaultValue={student.birthDate.toISOString().slice(0,10)} name="birthDate" required type="date"/><button className="rounded-lg border border-emerald-700 px-3 py-2 font-semibold text-emerald-800 md:col-span-3">Salvar dados do aluno</button>{message?<p className="text-sm md:col-span-3">{message}</p>:null}</form>}
