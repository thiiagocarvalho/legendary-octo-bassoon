'use client';
import { useEffect, useState } from 'react';
import { formatBirthdayDayMonth } from '../../lib/birthday-display';
type Person={id:string;fullName:string};type Birthday=Person&{birthDate:string};type Dashboard={activeStudents:number;pendingInvoices:number;receivedCents:number;monthlyForecasts:{label:string;cents:number}[];occupancyPercent:number;birthdays:Birthday[];studentsWithoutBooking:Person[];lowFrequency:Person[]};type Metric=[string,string];
const money=(cents:number)=>`R$ ${(cents/100).toFixed(2).replace('.',',')}`;
export function DashboardCards(){const [data,setData]=useState<Dashboard|null>(null);useEffect(()=>{fetch('/api/admin/dashboard').then(async (r:Response)=>r.ok&&setData(await r.json()));},[]);const values:Metric[]=data?[['Alunos ativos',String(data.activeStudents)],['Mensalidades pendentes',String(data.pendingInvoices)],['Valor recebido até agora',money(data.receivedCents)],...data.monthlyForecasts.map((item:Dashboard['monthlyForecasts'][number]):Metric=>[`Receita prevista — ${item.label}`,money(item.cents)]),['Ocupação da semana',`${data.occupancyPercent}%`]]:[['Alunos ativos','—'],['Mensalidades pendentes','—'],['Valor recebido até agora','—'],['Receita prevista','—'],['Ocupação da semana','—']];const alert=(title:string,tip:string,people:Person[],empty:string)=><section title={tip} className="rounded-xl border bg-white p-4"><h2 className="font-bold">{title}</h2>{people.length?<ul className="mt-2 list-disc pl-5">{people.map((person:Person)=><li key={person.id}>{person.fullName}</li>)}</ul>:<p className="mt-2 text-slate-600">{empty}</p>}</section>;const birthdays=data?.birthdays??[];return <><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{values.map(([label,value]:Metric)=><article title={`Indicador: ${label}`} className="rounded-xl border bg-white p-4" key={label}><strong className="capitalize">{label}</strong><p className="mt-2 text-2xl font-bold text-emerald-700">{value}</p></article>)}</div><div className="mt-6 grid gap-4 lg:grid-cols-3"><section title="Mostra alunos que fazem aniversário nos próximos 7 dias." className="rounded-xl border bg-white p-4"><h2 className="font-bold">Aniversariantes</h2>{birthdays.length?<ul className="mt-2 list-disc pl-5">{birthdays.map((person:Birthday)=><li key={person.id}>{person.fullName} · {formatBirthdayDayMonth(person.birthDate)}</li>)}</ul>:<p className="mt-2 text-slate-600">Nenhum aniversariante nos próximos 7 dias.</p>}</section>{alert('Sem reserva nesta semana','Lista os alunos ativos que ainda não têm aula marcada nesta semana.',data?.studentsWithoutBooking??[],'Todos os alunos ativos têm reserva.')}{alert('Baixa frequência','Lista alunos com menos de 4 presenças nos últimos 28 dias.',data?.lowFrequency??[],'Nenhum aluno abaixo de 4 presenças nos últimos 28 dias.')}</div></>;}
