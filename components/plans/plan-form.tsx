'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export function PlanForm(){const router=useRouter();const [message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const price=Number(String(data.monthlyPrice).replace(',','.'))*100;const response=await fetch('/api/admin/plans',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...data,monthlyPriceCents:Math.round(price)})});setMessage(response.ok?'Plano cadastrado.':'Confira os dados.');if(response.ok){e.currentTarget.reset();router.refresh();}}return <form className="grid gap-3 rounded-xl border bg-white p-5 md:grid-cols-4" onSubmit={submit}><input className="rounded-lg border px-3 py-2" name="name" placeholder="Plano 2x por semana" required/><input className="rounded-lg border px-3 py-2" name="monthlyPrice" placeholder="Valor mensal, ex.: 350" required type="number" min="1" step="0.01"/><input className="rounded-lg border px-3 py-2" name="weeklyBookingLimit" defaultValue="2" required type="number" min="1" max="7"/><button className="rounded-lg bg-emerald-700 px-4 py-2 font-semibold text-white">Cadastrar plano</button>{message?<p className="text-sm md:col-span-4">{message}</p>:null}</form>}
