'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export function ClassSlotForm() {
  const router = useRouter(); const [message, setMessage] = useState('');
  async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const formElement=e.currentTarget; const form = new FormData(formElement); const r = await fetch('/api/admin/class-slots',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(form))}); setMessage(r.ok?'Turma criada.':'Verifique os dados.'); if(r.ok){formElement.reset();router.refresh();} }
  return <form className="grid gap-3 rounded-xl border bg-white p-5 md:grid-cols-4" onSubmit={submit}><label className="grid gap-1 text-sm font-medium">Dia da semana<select className="rounded border p-2" name="weekday"><option value="1">Segunda-feira</option><option value="2">Terça-feira</option><option value="3">Quarta-feira</option><option value="4">Quinta-feira</option><option value="5">Sexta-feira</option></select></label><label className="grid gap-1 text-sm font-medium">Horário de início<input className="rounded border p-2" name="startsTime" required type="time"/></label><label className="grid gap-1 text-sm font-medium">Duração da aula (minutos)<input className="rounded border p-2" defaultValue="60" name="duration" required type="number"/></label><label className="grid gap-1 text-sm font-medium">Número de vagas<input className="rounded border p-2" defaultValue="4" name="capacity" required type="number"/></label><button className="rounded bg-emerald-700 px-3 py-2 font-semibold text-white md:col-span-4">Criar turma</button>{message?<p aria-live="polite" className="md:col-span-4">{message}</p>:null}</form>;
}
